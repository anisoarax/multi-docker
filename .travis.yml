language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t anisoarax/react-test -f ./client/Dockerfile.dev ./client
script:
  - docker run -e CI=true anisoarax/react-test npm test
after_success:
  - docker build -t anisoarax/multi-docker-client ./client
  - docker build -t anisoarax/multi-docker-nginx ./nginx
  - docker build -t anisoarax/multi-docker-server ./server
  - docker build -t anisoarax/multi-docker-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push anisoarax/multi-docker-client
  - docker push anisoarax/multi-docker-nginx
  - docker push anisoarax/multi-docker-server
  - docker push anisoarax/multi-docker-worker
